# указание на основе какого образа происходит сборка
FROM ubuntu:20.04

# указание рабочей директории
WORKDIR /home/Dranikf

# копирование
COPY script.sh script.sh

# запуск произвольной программы
RUN apt-get update && apt-get install -y vim wget curl git
# в данном случае мы обновляем индекс и уснанавливаем новые программы
# при установке паектов
# командная строка спросит
# Do you want to continue? [Y/n] Abort.
# для того, чтобы по умолчанию ответить y
# нужно казать опцию -y